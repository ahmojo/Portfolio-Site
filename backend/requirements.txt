# Pinned for reproducible Docker builds on the E2.1 Micro VM.
# Plain `uvicorn` (no [standard] extras) drops httptools/websockets/watchfiles
# (~3 MB + a few native wheels). The stdlib asyncio loop is plenty for a
# low-traffic portfolio on a 0.125 OCPU box.
fastapi==0.138.1
uvicorn==0.49.0
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
